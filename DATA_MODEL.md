# 데이터 구조 점검 결과

## 결론

문서(`SHOP.md`)에는 Cloudflare D1 구조가 정의되어 있었지만, 기존 구현에는 D1 바인딩과 테이블이 없었습니다. 상품은 `src/index.js` 상수에 고정되어 있고 장바구니와 주문은 브라우저 `localStorage`에만 저장됩니다. 따라서 브라우저를 바꾸거나 저장소를 지우면 데이터가 사라지고, 실제 사용자·주문 이력도 서버에 남지 않는 상태였습니다.

이번 점검에서 `migrations/0001_initial.sql`을 추가했습니다. D1에 적용하면 문서에 정의된 다섯 테이블과 외래 키, 중복 장바구니 방지, 수량·금액 검증, 조회 인덱스가 생성됩니다.

추가 점검에서 상품 분류명이 상품마다 반복 저장되는 문제를 확인했습니다. `0002`~`0004` 마이그레이션으로 `categories` 테이블을 만들고 `products.category_id` 외래 키로 바꿨으며, 중복 문자열 컬럼을 제거하고 분류 없는 상품 입력도 차단했습니다.

## 점검 항목별 결과

- 중복 정보: 상품 분류명을 `products`에 반복 저장하던 부분을 `categories` 한 곳으로 통합했습니다. `order_items.price`는 중복이 아니라 구매 당시 가격을 보존하는 의도적인 스냅샷입니다.
- 구매 당시 가격: `order_items.price`에 주문 시점 가격을 저장하고 있어 상품 가격이 바뀌어도 과거 주문 금액이 바뀌지 않습니다.
- 외래 키: 장바구니→사용자·상품, 주문→사용자, 주문 항목→주문·상품, 상품→분류 연결을 정의했습니다.
- 분류 외래 키: `products.category_id`가 `categories.id`를 참조하며, 분류명 문자열은 `categories.name`만 보존합니다.
- 인덱스: 사용자별 주문, 주문 상태별 조회, 주문 항목의 주문·상품 조회, 상품 분류 조회에 인덱스를 추가했습니다. 이메일·장바구니 복합키·주문 ID는 UNIQUE/PRIMARY KEY가 자동 인덱스를 제공합니다.

## 적용한 보완

- `products`: 가격·상품명·이미지 경로를 필수값으로 하고 음수 가격을 차단했습니다.
- `users`: 이메일을 대소문자 구분 없이 유일하게 만들고 비밀번호 원문이 아닌 해시만 저장하도록 했습니다.
- `cart_items`: `(user_id, product_id)`를 유일하게 해 같은 상품이 여러 줄로 쌓이지 않게 했고, 수량을 1~99로 제한했습니다.
- `orders`: 주문 상태를 `pending`/`paid`로 제한하고 총액을 음수로 저장할 수 없게 했습니다.
- `order_items`: 구매 당시 가격(`price`)을 별도로 보존해 상품 가격 변경이 과거 주문을 바꾸지 않게 했습니다.
- 외래 키와 인덱스를 추가해 고아 데이터와 느린 사용자별 주문 조회를 방지했습니다.

## 아직 연결하지 않은 부분

현재 단계에서는 로그인·결제 기능을 구현하지 않았으므로, 애플리케이션은 계속 기존 화면 동작을 유지합니다. 실제 D1 사용을 시작하려면 Cloudflare에서 D1 데이터베이스를 만든 뒤 `wrangler.jsonc`에 바인딩하고, 아래 명령으로 마이그레이션을 적용해야 합니다.

```text
npx wrangler d1 create korean-shop-db
npx wrangler d1 migrations apply korean-shop-db --remote
```

데이터베이스 ID가 정해지기 전에는 배포 설정에 임의의 ID를 넣지 않았습니다. 현재 코드에 D1을 연결하면 인증·주문 API까지 함께 설계해야 하므로, 다음 단계에서 별도로 진행하는 것이 안전합니다.
